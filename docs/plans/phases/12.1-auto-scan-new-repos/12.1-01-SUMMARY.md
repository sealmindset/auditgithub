---
phase: 12.1-auto-scan-new-repos
plan: 01
subsystem: backend
tags: [scheduler, apscheduler, cli, scan-automation]

# Dependency graph
requires:
  - phase: 5-schedule-execution
    provides: APScheduler infrastructure, ScheduledJob pattern
provides:
  - --new-repos-only CLI flag for scan_repos.py
  - scan_new_repos scheduled job (daily at 2 AM)
  - Environment variables: SCAN_NEW_REPOS_CRON, SCAN_NEW_REPOS_ENABLED
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns: [env-var-job-config, cli-filter-flag]

key-files:
  created: []
  modified:
    - scan_repos.py
    - src/api/scheduler.py
    - .env.sample

key-decisions:
  - "Filter repos in scan_repos.py after fetch, before dry_run check"
  - "Query database for each repo to check last_scanned_at (handles repos not yet in DB)"
  - "2 AM daily default cron, 2-hour timeout (shorter than full 4-hour scan)"

patterns-established:
  - "New scheduled job pattern: _add_job() + async _run_* handler"

issues-created: []

# Metrics
duration: 2 min
completed: 2026-01-17
---

# Phase 12.1 Plan 01: New Repo Auto-Scanner Summary

**Add 24-hour scheduled job to automatically scan repositories that have never been scanned before**

## Performance

- **Duration:** 2 min
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- Added `--new-repos-only` CLI flag to scan_repos.py that filters to repos with `last_scanned_at IS NULL`
- Added `scan_new_repos` scheduled job to scheduler.py following existing APScheduler patterns
- Documented scheduler environment variables in .env.sample

## Task Commits

1. **Task 1: Add --new-repos-only flag** - `fafe08c` (feat)
2. **Task 2: Add scan_new_repos job** - `9d14889` (feat)
3. **Task 3: Update environment documentation** - `6f0b1c1` (docs)

## Files Modified

- `scan_repos.py` - Added --new-repos-only argument and filtering logic after repo fetch
- `src/api/scheduler.py` - Added scan_new_repos job config and _run_scan_new_repos handler
- `.env.sample` - Added scheduler configuration section with all job env vars

## Decisions Made

- **Filter after fetch**: Query database for each repo after GitHub fetch to check if `last_scanned_at` is NULL
- **Include non-DB repos**: Repos not yet in database are also considered "new"
- **Shorter timeout**: 2-hour timeout vs 4-hour for full scans (new repos should be fewer)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Verification

- [x] `--new-repos-only` flag appears in `python scan_repos.py --help`
- [x] Scheduler module syntax validates correctly
- [x] Environment variables documented in .env.sample

---
*Phase: 12.1-auto-scan-new-repos*
*Completed: 2026-01-17*
