Like to enhance the API Audit capability even more. Let's add a way to:

1. Create and maintain an API Path Dictionary, were the analyst can add words/phrases used in the path the fuzzing. For example, soap, utility, snint, oauth2, ar, v1, capture-refund, services, click-updates, finalize-route

Like: https://snapidev-scus.example-org.com/snint/services/v1/click-update/finalize-route

So that the AI Agent and the scanner can improve its ability to findings of the every possible paths or routes or query or parameters or fragment, etc.

2. Create and maintain an API URI/URL library for the AI Agent to cross reference and use to learn and understand the URI/URL schema when discovering the paths 