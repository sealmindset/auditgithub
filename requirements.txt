requests>=2.31.0
tomli>=2.0.0  # For TOML parsing
urllib3>=2.0.0  # For retry/backoff in requests
python-dotenv>=1.0.0  # For .env file support
semgrep>=1.0.0  # For static code analysis
tree-sitter>=0.21.0  # For AST parsing
tree-sitter-kotlin>=1.0.0  # Kotlin grammar for tree-sitter
setuptools<81.0.0  # Avoid pkg_resources deprecation in newer versions
dependency-check

# AI Agent dependencies
openai>=1.0.0  # For OpenAI GPT-4 integration
anthropic>=0.18.0  # For Claude integration
psutil>=5.9.0  # For system metrics collection

# Note: Ensure these are installed in your environment:
# - git
# - npm (for Node.js projects)
# - go (for Go projects)
# - ruby (for Ruby projects)
# - java (for Java projects)
safety
pip-audit
# Note: The following tools need to be installed separately:
# - npm audit (comes with Node.js)
# - govulncheck (go install golang.org/x/vuln/cmd/govulncheck@latest)
# - bundle-audit (gem install bundler-audit)
# - OWASP Dependency-Check (https://owasp.org/www-project-dependency-check/)


# API Dependencies
fastapi>=0.100.0
httpx>=0.24.0  # Async HTTP client for API testing
uvicorn>=0.23.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
pydantic>=2.0.0
python-multipart>=0.0.6
diagrams>=0.23.0
apscheduler>=3.10.0  # For scheduled tasks (self-annealing, backups, scans)

# Authentication Dependencies
authlib>=1.3.0
python-jose[cryptography]>=3.3.0
cachetools>=5.3.0
pydantic-settings>=2.0.0
itsdangerous>=2.1.0  # Required for Starlette SessionMiddleware

# RBAC Caching & Session Management
redis>=5.0.0  # Redis client for permission caching, token blacklist, session metadata
hiredis>=2.2.0  # C parser for Redis performance
schedule>=1.2.0  # Periodic job scheduler for session cleanup
slowapi>=0.1.9  # Rate limiting for API protection

# Export/Report Generation
reportlab>=4.0.0  # For PDF generation
python-docx>=0.8.11  # For DOCX generation
google-genai>=0.2.0  # New Google GenAI SDK (replaces deprecated google-generativeai)

# Logging & Observability
loguru>=0.7.0  # Structured logging with HTTP transport
minio>=7.2.0  # S3-compatible storage client for log fallback

# Testing Dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0  # Code coverage for tests
