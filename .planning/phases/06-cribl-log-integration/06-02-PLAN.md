---
phase: 06-cribl-log-integration
plan: 02
type: execute
---

<objective>
Convert application-level logging from Python's logging module to Loguru across all API routers for unified Cribl integration.

Purpose: Ensure all application logs (errors, warnings, info) from API routers flow through Cribl for centralized observability, not just audit events.
Output: All 108 logger calls across 22+ routers converted to Loguru, integrated with request context propagation from Plan 06-01.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cribl-log-integration/06-01-SUMMARY.md
@src/api/routers/auth.py
@src/api/routers/api_audit.py
@src/api/routers/tenants.py
@src/api/utils/cribl_logger.py

**Tech stack available:** FastAPI, Loguru, Cribl HTTP logger with automatic context injection (from 06-01)
**Established patterns:** Structured audit logging with logger.bind(), request context automatically propagated via set_log_context()
**Constraining decisions:**
- Phase 6 Plan 01: Request context (request_id, org_id, user_id) automatically injected into all Loguru logs
- Phase 3: Audit events use logger.bind() for structured context

**Discovery findings:**
- 108 logger calls across routers use standard `logging` module (bypasses Cribl)
- Files with most logging: auth.py (5), api_audit.py (11), ai.py (47), tenants.py (7)
- Current pattern: `import logging; logger = logging.getLogger(__name__)`
- Target pattern: `from loguru import logger` (auto-configured to send to Cribl)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert high-traffic routers to Loguru (auth, tenants, api_audit)</name>
  <files>src/api/routers/auth.py, src/api/routers/tenants.py, src/api/routers/api_audit.py</files>
  <action>For each file:
1. Remove: `import logging` and `logger = logging.getLogger(__name__)`
2. Add at top after imports: `from loguru import logger`
3. Keep all logger.info(), logger.warning(), logger.error(), logger.debug() calls unchanged (Loguru API is compatible)
4. Add router-specific context using logger.bind() for router endpoints:
   - Example: logger.bind(router="auth", endpoint="login").info("User logged in")
5. Ensure exception logging includes exc_info:
   - Change: `logger.error(f"Error: {e}")`
   - To: `logger.exception(f"Error: {e}")` (auto-includes traceback)

DO NOT change log message content - only import and logger.exception() usage.
DO NOT add new log calls - this is a migration, not enhancement.

Files to modify:
- src/api/routers/auth.py (5 logger calls)
- src/api/routers/tenants.py (7 logger calls)
- src/api/routers/api_audit.py (11 logger calls)</action>
  <verify>python3 -m py_compile src/api/routers/auth.py src/api/routers/tenants.py src/api/routers/api_audit.py && grep "from loguru import logger" src/api/routers/auth.py src/api/routers/tenants.py src/api/routers/api_audit.py</verify>
  <done>auth.py, tenants.py, api_audit.py converted to Loguru with router-specific context binding</done>
</task>

<task type="auto">
  <name>Task 2: Convert remaining routers to Loguru (batch conversion)</name>
  <files>src/api/routers/jira.py, src/api/routers/github_sync.py, src/api/routers/scans.py, src/api/routers/projects.py, src/api/routers/attack_surface.py, src/api/routers/analytics.py, src/api/routers/feedback.py, src/api/routers/ai.py</files>
  <action>Apply same conversion pattern to remaining routers:
1. Replace `import logging` + `logger = logging.getLogger(__name__)` with `from loguru import logger`
2. Change exception logging to logger.exception() for traceback inclusion
3. Add router-specific context with logger.bind(router="router_name")

Files to modify (in order of priority):
- src/api/routers/ai.py (47 logger calls - highest volume)
- src/api/routers/github_sync.py (7 logger calls)
- src/api/routers/jira.py (3 logger calls)
- src/api/routers/scans.py (4 logger calls)
- src/api/routers/projects.py (6 logger calls)
- src/api/routers/attack_surface.py (5 logger calls)
- src/api/routers/analytics.py (2 logger calls)
- src/api/routers/feedback.py (1 logger call)

DO NOT modify routers that don't have logging imports (some may already use print() or have no logging).
Use grep to verify which routers actually import logging before modifying.</action>
  <verify>python3 -m py_compile src/api/routers/*.py && ! grep "import logging" src/api/routers/*.py | grep -v "# OLD:"</verify>
  <done>All remaining routers converted to Loguru, no standard logging imports remain</done>
</task>

<task type="auto">
  <name>Task 3: Update cribl_logger.py configuration and verify integration</name>
  <files>src/api/utils/cribl_logger.py, .env.example</files>
  <action>Update .env.example to document application logging (not just audit):
- Add comment above CRIBL_ENABLED: "# Cribl forwards all application logs (INFO+) and audit events"
- Update AUDIT_LOG_LEVEL comment: "# Minimum log level for application logs (DEBUG, INFO, WARNING, ERROR)"

Update src/api/utils/cribl_logger.py setup_cribl_logger() to ensure correct log level:
- Change Loguru add() level from "DEBUG" to respect AUDIT_LOG_LEVEL env var (default INFO)
- Add startup log: logger.info(f"Cribl logger initialized (enabled={_cribl_config.enabled}, min_level={level})")

DO NOT change the sink logic, queue, or batching - only startup configuration.
DO NOT add new features - this is configuration tuning only.</action>
  <verify>python3 -m py_compile src/api/utils/cribl_logger.py && grep "AUDIT_LOG_LEVEL" .env.example</verify>
  <done>Cribl logger configured to forward all application logs at INFO+ level, .env.example documented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 -m py_compile src/api/routers/*.py (all routers compile)
- [ ] ! grep "import logging" src/api/routers/*.py (no standard logging imports remain)
- [ ] grep "from loguru import logger" src/api/routers/auth.py src/api/routers/tenants.py (converted)
- [ ] grep "logger.exception" src/api/routers/auth.py (exception logging with traceback)
- [ ] grep "AUDIT_LOG_LEVEL" .env.example (documented)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- All 108 logger calls across routers converted to Loguru
- No `import logging` statements remain in src/api/routers/
- Exception logging uses logger.exception() for automatic traceback inclusion
- Router-specific context added via logger.bind(router="name")
- Application logs now flow to Cribl with automatic request context (request_id, org_id, user_id)
- .env.example documents application logging configuration
- No Python syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-cribl-log-integration/06-02-SUMMARY.md`:

---
phase: 06-cribl-log-integration
plan: 02
subsystem: logging, routers
tags: [cribl, loguru, application-logs, logger-migration]

# Dependency graph
requires:
  - phase: 06-01
    provides: Request context propagation (request_id, org_id, user_id) via set_log_context()
provides:
  - All 108 application logger calls converted to Loguru
  - Application logs forwarded to Cribl (not just audit events)
  - Router-specific context binding for log filtering
  - Exception tracebacks in centralized logs
affects: [06-03, 06-04]

# Tech tracking
tech-stack:
  added: []
  patterns: [Logger migration from logging to Loguru, Router context binding with logger.bind(), Exception logging with logger.exception()]

key-files:
  created: []
  modified: [src/api/routers/auth.py, src/api/routers/tenants.py, src/api/routers/api_audit.py, src/api/routers/ai.py, src/api/routers/github_sync.py, src/api/routers/jira.py, src/api/routers/scans.py, src/api/routers/projects.py, src/api/routers/attack_surface.py, src/api/routers/analytics.py, src/api/routers/feedback.py, src/api/utils/cribl_logger.py, .env.example]

key-decisions: []
patterns-established: []
issues-created: []

# Metrics
duration: [TBD]
completed: [TBD]
---

# Phase 6 Plan 2: Application Logger Migration Summary

**[One-line substantive summary of what shipped]**

## Accomplishments

- Converted 108 logger calls across 11 routers from logging to Loguru
- Migrated auth.py, tenants.py, api_audit.py, ai.py, github_sync.py, jira.py, scans.py, projects.py, attack_surface.py, analytics.py, feedback.py
- Changed exception logging to logger.exception() for automatic traceback inclusion
- Added router-specific context binding with logger.bind(router="name")
- Updated .env.example to document application logging configuration
- Configured Cribl logger to respect AUDIT_LOG_LEVEL for minimum log level

## Files Created/Modified

**Modified:**
- `src/api/routers/auth.py` - Converted 5 logger calls to Loguru
- `src/api/routers/tenants.py` - Converted 7 logger calls to Loguru
- `src/api/routers/api_audit.py` - Converted 11 logger calls to Loguru
- `src/api/routers/ai.py` - Converted 47 logger calls to Loguru
- `src/api/routers/github_sync.py` - Converted 7 logger calls to Loguru
- `src/api/routers/jira.py` - Converted 3 logger calls to Loguru
- `src/api/routers/scans.py` - Converted 4 logger calls to Loguru
- `src/api/routers/projects.py` - Converted 6 logger calls to Loguru
- `src/api/routers/attack_surface.py` - Converted 5 logger calls to Loguru
- `src/api/routers/analytics.py` - Converted 2 logger calls to Loguru
- `src/api/routers/feedback.py` - Converted 1 logger call to Loguru
- `src/api/utils/cribl_logger.py` - Configured log level from AUDIT_LOG_LEVEL env var
- `.env.example` - Documented application logging configuration

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 06-03-PLAN.md (Sensitive Data Redaction)
</output>
