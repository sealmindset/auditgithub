---
phase: 06-cribl-log-integration
plan: 01
type: execute
---

<objective>
Implement request lifecycle logging middleware with automatic correlation IDs and context propagation.

Purpose: Enable forensic analysis of requests by logging entry, exit, errors, and duration with complete context (user, tenant, request ID) automatically injected into all log events.
Output: Request logging middleware integrated with Cribl, automatic request context propagation to all downstream log calls.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-security-sessions/05-03-SUMMARY.md
@.planning/phases/03-rbac-system/03-03-SUMMARY.md
@src/api/main.py
@src/api/utils/cribl_logger.py
@src/auth/middleware.py

**Tech stack available:** FastAPI, Loguru, Cribl HTTP logger, Redis, SessionMiddleware, SecurityHeadersMiddleware
**Established patterns:** Middleware for cross-cutting concerns, structured audit logging with Cribl integration, request context via contextvars
**Constraining decisions:**
- Phase 3: Cribl integration for audit logging with automatic context injection
- Phase 5: Rate limiting with Redis, security headers middleware, session activity tracking
- Existing: cribl_logger.py provides log_context() and set_log_context() for context propagation

**Current state:**
- Cribl logger infrastructure exists but only used for audit events
- Middleware exists (OrganizationContextMiddleware, TenantMiddleware, SessionActivityMiddleware) but don't log to Cribl
- No request correlation IDs
- No request entry/exit logging
- Application logs don't have tenant/user context

**Discovery findings:**
- 108 logger calls across routers bypass Cribl (using standard logging, not Loguru)
- Middleware failures logged locally but not centralized
- No structured request/response lifecycle events
- Missing correlation IDs for distributed tracing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create request logging middleware with correlation IDs</name>
  <files>src/api/middleware/logging.py</files>
  <action>Create new middleware RequestLoggingMiddleware that:
- Generates UUID request_id for each request (use uuid.uuid4())
- Logs REQUEST_START event with: method, path, client_ip, user_agent, request_id, timestamp
- Captures response with: status_code, duration_ms, bytes_sent
- Logs REQUEST_END event with all request + response data
- Logs REQUEST_ERROR for unhandled exceptions with full traceback
- Uses loguru logger (not standard logging) to ensure Cribl integration
- Injects request_id into response headers (X-Request-ID) for client correlation
- Handles exceptions gracefully (catch Exception, log, re-raise)

Use Starlette BaseHTTPMiddleware pattern like SessionActivityMiddleware and SecurityHeadersMiddleware (see @src/auth/middleware.py).

Extract client IP from X-Forwarded-For header (fallback to request.client.host if not present).

DO NOT use response.body() - it consumes the stream. Use StreamingResponse wrapper if response introspection needed.</action>
  <verify>python3 -m py_compile src/api/middleware/logging.py</verify>
  <done>Middleware created with REQUEST_START, REQUEST_END, REQUEST_ERROR events using Loguru</done>
</task>

<task type="auto">
  <name>Task 2: Integrate request context propagation throughout request lifecycle</name>
  <files>src/api/middleware/logging.py, src/api/main.py</files>
  <action>Update RequestLoggingMiddleware to call set_log_context() at request start with:
- request_id (generated UUID)
- org_id (from request.state.org_id if exists)
- org_name (from request.state.org_name if exists)
- user_id (from request.state.user.sub if exists)
- session_id (from cookies if exists)

Call clear_log_context() after response sent (in finally block).

Import set_log_context and clear_log_context from src.api.utils.cribl_logger.

In src/api/main.py:
- Import RequestLoggingMiddleware from src.api.middleware.logging
- Mount BEFORE OrganizationContextMiddleware (needs to wrap all middleware for full logging)
- Order: RequestLoggingMiddleware → OrganizationContextMiddleware → TenantMiddleware → SessionActivityMiddleware → SecurityHeadersMiddleware

DO NOT use app.add_middleware() for ordering - use app.middleware("http") decorator for precise control. See existing middleware in main.py around line 180-190.</action>
  <verify>grep -n "RequestLoggingMiddleware" src/api/main.py && python3 -m py_compile src/api/main.py</verify>
  <done>Middleware integrated in main.py, set_log_context() propagates request_id/org_id/user_id to all downstream logs, clear_log_context() called after response</done>
</task>

<task type="auto">
  <name>Task 3: Add request logging to existing middleware that currently doesn't log</name>
  <files>src/api/middleware/tenant.py, src/api/main.py</files>
  <action>Update src/api/middleware/tenant.py TenantMiddleware to use Loguru logger (import from loguru import logger):

Replace 5 existing logger.debug/warning calls with structured Loguru calls:
- Tenant validation success: logger.info(f"Tenant validated: {tenant_id}")
- Tenant validation failure: logger.warning(f"Tenant validation failed: {tenant_id} - {reason}")
- Schema switching: logger.debug(f"Schema switched to: {schema_name}")

Update src/api/main.py OrganizationContextMiddleware to add logging:
- Organization context extracted: logger.debug(f"Organization context: {org_id}")
- Missing organization context: logger.warning("Request missing organization context")

Use logger.bind() to add middleware-specific context:
  logger.bind(middleware="TenantMiddleware", tenant_id=tenant_id).info("Tenant validated")

DO NOT add logging to SecurityHeadersMiddleware or SessionActivityMiddleware - they're performance-critical and already working correctly.</action>
  <verify>grep -n "from loguru import logger" src/api/middleware/tenant.py && python3 -m py_compile src/api/middleware/tenant.py src/api/main.py</verify>
  <done>TenantMiddleware and OrganizationContextMiddleware log validation events to Cribl with structured context</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 -m py_compile src/api/middleware/logging.py
- [ ] python3 -m py_compile src/api/middleware/tenant.py
- [ ] python3 -m py_compile src/api/main.py
- [ ] grep "RequestLoggingMiddleware" src/api/main.py (mounted in correct order)
- [ ] grep "set_log_context\|clear_log_context" src/api/middleware/logging.py (context propagation present)
- [ ] grep "from loguru import logger" src/api/middleware/tenant.py (converted from logging)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- RequestLoggingMiddleware generates UUID request_id for each request
- REQUEST_START, REQUEST_END, REQUEST_ERROR events logged to Cribl
- set_log_context() propagates request_id, org_id, user_id to all downstream logs
- TenantMiddleware and OrganizationContextMiddleware log to Cribl with structured context
- X-Request-ID header returned in responses
- No Python syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-cribl-log-integration/06-01-SUMMARY.md`:

---
phase: 06-cribl-log-integration
plan: 01
subsystem: logging, middleware
tags: [cribl, loguru, correlation-ids, request-lifecycle, context-propagation]

# Dependency graph
requires:
  - phase: 03-03
    provides: Cribl logger with log_context() and audit event integration
  - phase: 05-03
    provides: SessionActivityMiddleware, SecurityHeadersMiddleware patterns
provides:
  - Request correlation IDs (UUID per request)
  - REQUEST_START, REQUEST_END, REQUEST_ERROR events in Cribl
  - Automatic context propagation (request_id, org_id, user_id) to all logs
  - X-Request-ID response header for client correlation
affects: [06-02, 06-03, 06-04]

# Tech tracking
tech-stack:
  added: []
  patterns: [Request logging middleware, Correlation ID generation, Automatic context propagation via contextvars]

key-files:
  created: [src/api/middleware/logging.py]
  modified: [src/api/main.py, src/api/middleware/tenant.py]

key-decisions: []
patterns-established: []
issues-created: []

# Metrics
duration: [TBD]
completed: [TBD]
---

# Phase 6 Plan 1: Request Lifecycle Logging Summary

**[One-line substantive summary of what shipped]**

## Accomplishments

- Created RequestLoggingMiddleware with REQUEST_START, REQUEST_END, REQUEST_ERROR events
- Implemented automatic UUID request_id generation for correlation
- Integrated set_log_context() and clear_log_context() for automatic context propagation
- Converted TenantMiddleware to Loguru for Cribl integration
- Added structured logging to OrganizationContextMiddleware
- Mounted middleware in correct order for full request lifecycle coverage
- Added X-Request-ID response header for client-side correlation

## Files Created/Modified

**Created:**
- `src/api/middleware/logging.py` - RequestLoggingMiddleware with lifecycle events

**Modified:**
- `src/api/main.py` - Mounted RequestLoggingMiddleware, added logging to OrganizationContextMiddleware
- `src/api/middleware/tenant.py` - Converted from logging to Loguru, added structured context

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 06-02-PLAN.md (Application Logger Migration)
</output>
