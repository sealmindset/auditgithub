---
phase: 02-authentication-foundation
type: execute
---

<objective>
Set up OIDC/SSO foundation with Authlib and configure Entra ID and Okta providers.

Purpose: Establish the authentication infrastructure that all protected routes will depend on. Creates the module structure, installs dependencies, and configures multi-provider OIDC discovery.
Output: Working OAuth provider registry with both Entra ID and Okta configured, SessionMiddleware integrated into FastAPI app.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication-foundation/02-RESEARCH.md
@src/api/main.py
@src/api/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install authentication dependencies and create auth module structure</name>
  <files>requirements.txt, src/auth/__init__.py, src/auth/config.py, src/auth/providers.py, src/auth/middleware.py, src/auth/dependencies.py, src/auth/models.py</files>
  <action>
1. Add to requirements.txt:
   - authlib>=1.3.0
   - python-jose[cryptography]>=3.3.0
   - httpx>=0.27.0
   - cachetools>=5.3.0
   - pydantic-settings>=2.0.0
2. Create src/auth/ directory with __init__.py
3. Create empty module files: config.py, providers.py, middleware.py, dependencies.py, models.py
4. In src/auth/__init__.py, add exports: `from .providers import oauth, init_oauth` and `from .dependencies import get_current_user`

Why: Authlib is the standard OAuth/OIDC client library (see RESEARCH.md "Standard Stack"). python-jose is recommended by FastAPI for JWT validation. httpx is needed for async OIDC discovery. cachetools will cache JWKS with TTL. DO NOT use fastapi-oidc or custom OAuth implementations - they lack security features like PKCE and proper redirect_uri validation.
  </action>
  <verify>pip install -r requirements.txt succeeds, python -c "import authlib, jose, httpx, cachetools" works, ls src/auth/ shows all 6 files</verify>
  <done>All dependencies installed, src/auth/ module created with 6 files, imports work without errors</done>
</task>

<task type="auto">
  <name>Task 2: Configure Entra ID and Okta provider settings with OIDC discovery URLs</name>
  <files>src/auth/config.py, src/api/config.py, .env.example</files>
  <action>
1. In src/auth/config.py:
   - Import from pydantic_settings: BaseSettings
   - Create Settings class with fields:
     * session_secret: str (for SessionMiddleware)
     * entra_client_id: str
     * entra_client_secret: str
     * entra_tenant_id: str
     * okta_client_id: str
     * okta_client_secret: str
     * okta_domain: str (e.g., "dev-12345.okta.com")
   - Add @property entra_discovery_url returning f"https://login.microsoftonline.com/{self.entra_tenant_id}/v2.0/.well-known/openid-configuration"
   - Add @property okta_discovery_url returning f"https://{self.okta_domain}/.well-known/openid-configuration"
   - Add Config class with env_file = ".env"
   - Create singleton: settings = Settings()
2. Add to .env.example (NOT .env - credentials are not yet available):
   SESSION_SECRET=changeme-generate-with-openssl-rand-hex-32
   ENTRA_TENANT_ID=your-tenant-id-here
   ENTRA_CLIENT_ID=your-client-id
   ENTRA_CLIENT_SECRET=your-client-secret
   OKTA_DOMAIN=dev-12345.okta.com
   OKTA_CLIENT_ID=your-okta-client-id
   OKTA_CLIENT_SECRET=your-okta-client-secret

Why: Use server_metadata_url (OIDC discovery) instead of manual endpoint configuration - Authlib auto-fetches .well-known/openid-configuration and handles provider updates automatically (see RESEARCH.md "Don't Hand-Roll"). DO NOT hardcode authorization_endpoint, token_endpoint, etc. - discovery handles this correctly.
  </action>
  <verify>python -c "from src.auth.config import settings; print(settings.entra_discovery_url, settings.okta_discovery_url)" prints URLs correctly</verify>
  <done>Settings class created with all 7 fields plus 2 discovery URL properties, .env.example updated with placeholders, import works</done>
</task>

<task type="auto">
  <name>Task 3: Initialize OAuth providers with Authlib and add SessionMiddleware to FastAPI</name>
  <files>src/auth/providers.py, src/api/main.py</files>
  <action>
1. In src/auth/providers.py:
   - Import: from authlib.integrations.starlette_client import OAuth
   - Import: from src.auth.config import settings
   - Create global: oauth = OAuth()
   - Create function init_oauth():
     * Call oauth.register(name='entra', client_id=settings.entra_client_id, client_secret=settings.entra_client_secret, server_metadata_url=settings.entra_discovery_url, client_kwargs={'scope': 'openid profile email'})
     * Call oauth.register(name='okta', client_id=settings.okta_client_id, client_secret=settings.okta_client_secret, server_metadata_url=settings.okta_discovery_url, client_kwargs={'scope': 'openid profile email'})
2. In src/api/main.py (after line 79, before routers import):
   - Import: from starlette.middleware.sessions import SessionMiddleware
   - Import: from src.auth.providers import init_oauth
   - Import: from src.auth.config import settings as auth_settings
   - Add: app.add_middleware(SessionMiddleware, secret_key=auth_settings.session_secret, max_age=3600, https_only=False)  # Note: https_only=False for local dev, change to True in production
   - Call: init_oauth()

Why: Authlib's OAuth client automatically handles PKCE, state parameter, nonce, and code exchange (see RESEARCH.md "Architecture Patterns"). SessionMiddleware is required for OAuth flow state management between /login and /callback. DO NOT implement custom session storage yet - signed cookies are sufficient for auth flow. DO NOT set https_only=True in dev - it breaks localhost testing.
  </action>
  <verify>python -c "from src.auth.providers import oauth; print(list(oauth._clients.keys()))" prints ['entra', 'okta'], uvicorn src.api.main:app --reload starts without errors</verify>
  <done>OAuth providers registered with server_metadata_url for auto-discovery, SessionMiddleware added to FastAPI with 1-hour max_age, app starts successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -r requirements.txt` succeeds
- [ ] `python -c "import authlib, jose, httpx, cachetools"` works
- [ ] `python -c "from src.auth.config import settings; print(settings.entra_discovery_url)"` prints valid URL
- [ ] `python -c "from src.auth.providers import oauth; print(list(oauth._clients.keys()))"` shows ['entra', 'okta']
- [ ] `uvicorn src.api.main:app --reload` starts without import errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No import errors or missing dependencies
- OAuth providers configured with OIDC discovery
- SessionMiddleware integrated into FastAPI app
- .env.example template created (credentials not yet configured)
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-foundation/02-01-SUMMARY.md`:

# Phase 2 Plan 1: OIDC Foundation & Provider Setup Summary

**OIDC/SSO authentication foundation established with Authlib and multi-provider configuration**

## Accomplishments

- Installed Authlib, python-jose, httpx, cachetools for OIDC/JWT handling
- Created src/auth/ module with config, providers, middleware, dependencies, models
- Configured Entra ID and Okta with automatic OIDC discovery
- Integrated SessionMiddleware for OAuth flow state management

## Files Created/Modified

- `requirements.txt` - Added authentication dependencies
- `src/auth/__init__.py` - Module exports
- `src/auth/config.py` - Settings with Entra ID and Okta configuration
- `src/auth/providers.py` - OAuth provider registry with server_metadata_url
- `src/auth/middleware.py` - Empty (ready for JWT validation in 02-03)
- `src/auth/dependencies.py` - Empty (ready for get_current_user in 02-03)
- `src/auth/models.py` - Empty (ready for User model in 02-03)
- `src/api/main.py` - Added SessionMiddleware and init_oauth() call
- `.env.example` - Added auth configuration placeholders

## Decisions Made

- Use server_metadata_url for OIDC discovery (not manual endpoints)
- SessionMiddleware with 1-hour max_age (sufficient for auth flow)
- https_only=False for local dev (must change to True for production)

## Issues Encountered

None

## Next Step

Ready for 02-02-PLAN.md (Login Flow with PKCE)
</output>
